# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Realm Array String Example' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'RealmSwift', '~> 3.0'

  # Pods for Realm Array String Example

  target 'Realm Array String ExampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm Array String ExampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
